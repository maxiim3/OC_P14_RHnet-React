import styled from "styled-components"

export const HeaderNavLinksContainerStyled = styled.ul`
  // Wraps the links for navigation
  display: flex;
  flex-flow: row wrap;
  gap: clamp(4px, 2vw, 12px);
`